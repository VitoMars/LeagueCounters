import { Champion } from '../models/Champion';

export const getRecommendations = (allyTeam: Champion[], enemyTeam: Champion[]): Champion[] => {

  console.log({ allyTeam, enemyTeam })
  // Implementa la tua logica di raccomandazione qui
  // Potrebbe essere una chiamata a un servizio esterno di machine learning o una logica interna basata su regole
  return [];
};